// if

const isUserloggedIn = true

if(isUserloggedIn) {
    console.log(`User is logged In is ${isUserloggedIn}`);
}

// <, >, <=, >=, ==, ===, !=, !==,


let score = 5400;

if (score > 5000) {

    const power = "fly"
    console.log(`The power is ${power}`);
    
}
else {
    console.log(`You have no power`);
}


// const bal = 5000;
// if(bal >= 1000)  new_bal = 43, console.log("test", new_bal);

// if(bal >= 5000) console.log("perfect test", new_bal)